{
  "name": "stackset-resource",
  "version": "1.0.0",
  "description": "CloudFormation Custom Resource to create a S3 bucket in a secondary AWS region",
  "main": "index.js",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "clean": "rm -rf node_modules/ && rm -rf package/ && rm -rf build/",
    "test": "jest --config jestconfig.json --coverage"
  },
  "author": "AWS Solutions",
  "license": "Apache-2.0",
  "devDependencies": {
    "aws-lambda": "^1.0.5",
    "aws-sdk": "^2.606.0",
    "@types/node": "^13.1.8",
    "@types/jest": "^24.9.0",
    "aws-sdk-mock": "^5.0.0",
    "chai": "^4.2.0",
    "axios-mock-adapter": "^1.17.0",
    "jest": "^25.1.0",
    "sinon": "^8.1.0",
    "ts-jest": "^25.0.0",
    "typescript": "^3.7.5"
  },
  "dependencies": {
    "axios": "^0.19.0"
  }
}
