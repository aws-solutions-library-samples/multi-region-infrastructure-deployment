{
  "name": "rollback-change",
  "version": "1.1.0",
  "description": "Lambda Package to roll back the stage if the approval is rejected.",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf node_modules coverage",
    "test": "CI=true jest --config jestconfig.json --coverage --silent"
  },
  "author": "aws-solutions-builders",
  "license": "Apache-2.0",
  "dependencies": {
    "adm-zip": "~0.4.16"
  },
  "devDependencies": {
    "aws-sdk": "2.631.0",
    "jest": ""
  }
}
